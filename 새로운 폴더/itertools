from itertools import permutations

def permutation_search(elements):
    perms = permutations(elements)  # 順列の生成

    for perm in perms:
        print(perm)  # 順列の表示

# テスト用の要素リスト
elements = [1, 2, 3]

# 順列の全探索
permutation_search(elements)
