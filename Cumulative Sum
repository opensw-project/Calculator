def cumulative_sum(arr):
    n = len(arr)
    cum_sum = [0] * n
    cum_sum[0] = arr[0]
  
    for i in range(1, n):
        cum_sum[i] = cum_sum[i-1] + arr[i]

    return cum_sum

# テスト用の配列
arr = [1, 2, 3, 4, 5]

# 累積和の計算
result = cumulative_sum(arr)

# 結果の出力
print("累積和:", result)
